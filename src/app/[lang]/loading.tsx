export default function Loading() {
  return (
    <div className="w-full bg-[#f7f7f7] min-h-screen">
      {/* Minimal skeleton - Hero area only, static content renders instantly */}
      <section className="relative h-screen min-h-[700px] bg-gray-800 animate-pulse" />
    </div>
  );
}
